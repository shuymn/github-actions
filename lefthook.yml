colors: true
no_tty: false

pre-commit:
  parallel: true
  piped: true

  commands:
    lint:
      run: make lint
    gitleaks:
      run: gitleaks protect --staged

  skip:
    - merge: true
    - rebase: true
